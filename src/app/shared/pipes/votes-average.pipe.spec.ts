import { VotesAveragePipe } from './votes-average.pipe';

describe('VotesAveragePipe', () => {
  it('create an instance', () => {
    const pipe = new VotesAveragePipe();
    expect(pipe).toBeTruthy();
  });
});
